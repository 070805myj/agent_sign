# 基线算法规划

## FCFS 算法规则
- 必须为纯串行实现
- 按照任务到达顺序或编号顺序依次处理
- 严格遵循先来先服务原则，不允许并行优化

## SPT 算法规则  
- 必须采用事件驱动的单设备处理逻辑
- 优先选择处理时间最短的任务进行调度
- 每次只处理一个设备上的任务，避免并发冲突

## 结果唯一性
- 给定相同的输入数据，基线算法必须产生完全一致的输出结果
- 算法实现中避免任何随机性因素
- 确保基线算法的可验证性和可复现性