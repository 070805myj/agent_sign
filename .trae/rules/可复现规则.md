# 可复现规则

## 随机种子控制
- 所有随机操作必须统一使用全局随机种子
- 在程序入口处统一设置：
  - `random.seed()` 设置标准库随机种子
  - `np.random.seed()` 设置 NumPy 随机种子

## 随机操作范围
涉及随机操作的场景包括但不限于：
- 种群初始化
- 交叉操作
- 变异操作
- 轮盘赌选择等随机选择策略

## 可复现性验证
- 相同输入数据 + 相同随机种子 = 相同输出结果
- 算法调试和测试时必须记录使用的随机种子
- 所有实验报告必须包含随机种子信息